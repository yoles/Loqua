{
  "name": "loqua",
  "private": true,
  "packageManager": "pnpm@11.9.0",
  "scripts": {
    "build": "pnpm lint:deps && turbo run build",
    "test": "turbo run test",
    "lint:deps": "depcruise packages apps services tooling --config .dependency-cruiser.cjs"
  },
  "devDependencies": {
    "dependency-cruiser": "^18.0.0",
    "turbo": "^2.5.0",
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  }
}
