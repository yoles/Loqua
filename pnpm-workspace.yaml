packages:
  - "packages/*"
  - "apps/*"
  - "services/*"
  - "tooling/*"
allowBuilds:
  esbuild: true
  # deps natives Node de transformers.js — inutiles (adapter navigateur only)
  onnxruntime-node: false
  protobufjs: false
  sharp: false
  workerd: true # runtime de wrangler dev (services/api)
