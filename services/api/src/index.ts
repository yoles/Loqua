// Backend fin (Hono sur Cloudflare Workers) : proxy LLM ZDR texte seul.
// Implémenté au Sprint 2 (lot 2.4) — le serveur ne voit jamais l'audio (invariant §1).
export const API_NAME = '@loqua/api' as const;
