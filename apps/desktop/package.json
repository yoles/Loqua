{
  "name": "@loqua/desktop",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/build-sidecar.mjs && tauri dev",
    "bundle": "node scripts/build-sidecar.mjs --release && tauri build",
    "test": "cargo test --manifest-path src-tauri/Cargo.toml"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.9.0"
  }
}
