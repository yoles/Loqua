{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Surface minimale : uniquement le socle core (invoke des commandes déclarées).",
  "windows": ["main"],
  "permissions": ["core:default"]
}
